class Node():
	__name = "00"
	__adj_nodes = []

	def __init__(self, name, nodes):
		self.__name = name
		self.__adj_nodes = nodes

	def move_between_nodes(self, dest_node):
		if self.__name = "03":
			if dest_node.__name in self.__adj_nodes:
				if dest_node.__name == "02":
					# Go down 1 unit, go right 1 unit
				elif dest_node.__name == "26":
					# Go up 1 unit, go right 1 unit
				elif dest_node.__name == "11":
					# Circle(radius = 1, start = 180, end = 225)
				elif dest_node.__name == "15":
					# Circle(radius = 1, start = 180, end = 135)
				else:
					#invalid node
			else:
				#invalid node

			return

		if self.__name = "11":
			if dest_node.__name in self.__adj_nodes:
				if dest_node.__name == "02":
					# Go down 1 unit, go right 1 unit
				elif dest_node.__name == "26":
					# Go up 1 unit, go right 1 unit
				elif dest_node.__name == "11":
					# Circle(radius = 1, start = 180, end = 225)
				elif dest_node.__name == "15":
					# Circle(radius = 1, start = 180, end = 135)
				else:
					#invalid node
			else:
				#invalid node

			return
