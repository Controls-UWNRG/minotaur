#!/bin/bash
# Setup NRG Minotaur script

cat << EOF 

####################################
      UW_NRG Minotaur Setup 
####################################
* Xcode command line tools is needed to install homebrew
* Homebrew must be installed

EOF

# Check to see if homebrew is installed

# Install Python 2.7
#brew install python
# Install pip 
#python get-pip.py

# Install pyserial, pygtk, numpy
#sudo pip install pyserial
#sudo pip install numpy
#brew install pygtk

#brew tap homebrew/science
#brew install opencv

#export PATH=$PATH:/usr/local/lib/python2.7/site-packages
